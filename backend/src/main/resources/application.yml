server:
  port: 8080

spring:
  application:
    name: rag-demo

app:
  ollama:
    base-url: http://localhost:11434
    chat-model: llama3.2
    embedding-model: nomic-embed-text

management:
  endpoints:
    web:
      exposure:
        include: health,info
